// Local, offline Go module for the compliance starter. stdlib-only — no external deps,
// so `go test ./...` runs fully offline. Copy the package into your project and adjust the path.
module compliance-starter

go 1.21
